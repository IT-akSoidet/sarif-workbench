"""py-bookstore — intentionally vulnerable Flask app for SAST benchmarking.

This is NOT a product. It is a controlled reference set of known CWEs placed in
fixed locations so that Bandit / Semgrep / CodeQL output can be compared inside
SARIF Workbench. Every intentional weakness is marked with `# VULN: CWE-XXX`.
"""

__all__ = []
